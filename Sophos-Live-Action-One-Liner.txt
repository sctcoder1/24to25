powershell.exe -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command "function Get-ExistingUpgradeTasks { @(Get-ScheduledTask -ErrorAction Stop | Where-Object { $_.TaskPath -eq '\' -and $_.TaskName -in @('Win11-25H2-AtStartup','Win11-25H2-Retry') }); }; function Assert-UpgradeTasksIdle { param([object[]]$Tasks); if (@($Tasks | Where-Object { $_.State -eq 'Running' }).Count) { throw 'An existing upgrade task is running. Wait for it to finish; nothing has been stopped or deleted.'; }; }; function Assert-NotReparsePoint { param([string]$Path); if (Test-Path -LiteralPath $Path) { if ((Get-Item -LiteralPath $Path -Force -ErrorAction Stop).Attributes -band [IO.FileAttributes]::ReparsePoint) { throw ('Refusing a redirected path: '+$Path); }; }; }; function Invoke-SophosBootstrap { $ErrorActionPreference='Stop'; $r='C:\ProgramData\Win11-25H2'; $identity=[Security.Principal.WindowsIdentity]::GetCurrent(); $principal=New-Object Security.Principal.WindowsPrincipal($identity); if (-not $principal.IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)) { throw 'Run from Sophos as SYSTEM or from an elevated administrator CMD.'; }; $mutex=New-Object Threading.Mutex($false,'Global\Win11-25H2-Upgrade'); $locked=$false; try { try { $locked=$mutex.WaitOne(0); } catch [Threading.AbandonedMutexException] { $locked=$true; }; if (-not $locked) { throw 'Another Sophos or TeamViewer upgrade worker is active. Nothing has been changed.'; }; $existing=@(Get-ExistingUpgradeTasks); Assert-UpgradeTasksIdle -Tasks $existing; foreach ($flag in @('HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Component Based Servicing\RebootPending','HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\WindowsUpdate\Auto Update\RebootRequired')) { if (Test-Path -LiteralPath $flag) { throw 'Windows reports a pending update restart. Let patching finish and restart before deploying this workflow.'; }; }; $installer=New-Object -ComObject Microsoft.Update.Installer; if ($installer.IsBusy) { throw 'Windows Update is busy installing updates. Let patching finish before deploying this workflow.'; }; Assert-NotReparsePoint -Path $r; $null=New-Item -ItemType Directory -Path $r -Force; $acl=Get-Acl -LiteralPath $r; $acl.SetSecurityDescriptorSddlForm('O:BAG:BAD:P(A;OICI;FA;;;SY)(A;OICI;FA;;;BA)'); Set-Acl -LiteralPath $r -AclObject $acl; $stage=Join-Path $r ('staging-'+[guid]::NewGuid().ToString('N')); $null=New-Item -ItemType Directory -Path $stage; $base='https://raw.githubusercontent.com/sctcoder1/24to25/04e1e8e75c3c03586d832290ee52c1f48c714e10/'; $files=@{'Upgrade-25H2.ps1'='FE151F18A4E29927CB42A33533F56DE34FAF222D19F70642B433A671DF51E416';'Win11-25H2-Launcher.bat'='1ECFA11C3798D894DE76AA369025CD901162D9DFF0FC95BA1E3891E02212D42D'}; [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12; foreach ($name in $files.Keys) { $download=Join-Path $stage $name; Invoke-WebRequest -UseBasicParsing -Uri ($base+$name) -OutFile $download -TimeoutSec 60; if ((Get-FileHash -LiteralPath $download -Algorithm SHA256).Hash -ne $files[$name]) { throw ($name+' SHA256 mismatch. Existing tasks and deployed files have not been replaced.'); }; }; $worker=Join-Path $stage 'Upgrade-25H2.ps1'; $fixed=[IO.File]::ReadAllText($worker).Replace('    Write-Output $line','    Write-Host $line'); [IO.File]::WriteAllText($worker,$fixed,(New-Object Text.UTF8Encoding($false))); if ((Get-FileHash -LiteralPath $worker -Algorithm SHA256).Hash -ne 'A53BF4DCEA3778E43CF81B1C2EFFF2C24CC9CB948F388C0210C807DC7996A83D') { throw 'Corrected worker SHA256 mismatch. Existing deployment has not been replaced.'; }; Assert-UpgradeTasksIdle -Tasks @(Get-ExistingUpgradeTasks); foreach ($name in $files.Keys) { Assert-NotReparsePoint -Path (Join-Path $r $name); }; foreach ($name in $files.Keys) { $target=Join-Path $r $name; if (Test-Path -LiteralPath $target) { Copy-Item -LiteralPath $target -Destination (Join-Path $stage ($name+'.previous')) -Force; }; Copy-Item -LiteralPath (Join-Path $stage $name) -Destination $target -Force; }; $action=New-ScheduledTaskAction -Execute (Join-Path $env:SystemRoot 'System32\cmd.exe') -Argument '/d /c C:\ProgramData\Win11-25H2\Win11-25H2-Launcher.bat'; $system=New-ScheduledTaskPrincipal -UserId 'SYSTEM' -LogonType ServiceAccount -RunLevel Highest; $settings=New-ScheduledTaskSettingsSet -MultipleInstances IgnoreNew -StartWhenAvailable -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -ExecutionTimeLimit ([timespan]::Zero); $startup=New-ScheduledTaskTrigger -AtStartup; $retry=New-ScheduledTaskTrigger -Once -At ((Get-Date).AddMinutes(3)) -RepetitionInterval (New-TimeSpan -Hours 3); $null=Register-ScheduledTask -TaskPath '\' -TaskName 'Win11-25H2-AtStartup' -Action $action -Principal $system -Settings $settings -Trigger $startup -Force; $null=Register-ScheduledTask -TaskPath '\' -TaskName 'Win11-25H2-Retry' -Action $action -Principal $system -Settings $settings -Trigger $retry -Force; } finally { if ($locked) { $mutex.ReleaseMutex(); }; $mutex.Dispose(); }; Start-ScheduledTask -TaskPath '\' -TaskName 'Win11-25H2-Retry' -ErrorAction Stop; Write-Output 'SYSTEM BAT tasks registered; first run requested. Existing logs/state retained. Check Launcher.log and Upgrade-25H2.log for execution results.'; }; Invoke-SophosBootstrap;"
